apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: r-webapp-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: r-webapp-service
            port:
              number: 80